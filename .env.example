# Create a Bybit API key with READ-ONLY permissions. Never use a trading key here.
BYBIT_API_KEY=
BYBIT_API_SECRET=
# Optional: set a default linear perpetual symbol, for example ETHUSDT.
BYBIT_SYMBOL=ETHUSDT
# Local-only server port
PORT=8787
